# World Corner 发布准则 v1.1

## 1. YAML 语法防火墙
- title 字段禁止嵌套同种引号，统一使用中文「」。
- draft 默认为 false，除非明确要求。
- tags 保持小写英文，逗号分隔。

## 2. 排版工程学
- 架构图/流程图必须用 ```text 代码块包裹，严禁直放正文。
- 代码块必须声明语言（python, bash, yaml）。

## 3. 摘要准则
- 仅从 `>` 引用块提取金句，上限 3 条。
- 无引用块时跳过金句，只发标题+链接。
- 全文链接格式：`https://worldcorner.xyz/posts/{slug}/`
- 链接中的 slug 从文件名（不含扩展名）自动生成。

## 4. CI/CD 环境锁定
- Hugo: 0.146.0
- Node: FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true
